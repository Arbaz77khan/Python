# to import any file we need to add 'm' letter in starting of the filename.
import mFileImport1

#above code will execute all program written in mFileImport1, but if we have to execute only some part of code we hade if code written in mFileImport1
mFileImport1.greet('arbaz')
